name: crucible-ci

on:
  pull_requesst:
    branches: [ main ]
    paths:
    - LICENSE
    - '**.md'

jobs:
  crucible-ci-complete:
    runs-on: ubuntu-latest
    steps:
    - run: 'echo "No build required" '
